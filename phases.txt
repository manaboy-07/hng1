[11:01 PM]:bar_chart: **Grading Report**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
:dart: Score: 12/60 (20.0%)
:white_check_mark: Passed: 2/10
:x: Failed: 8/10
:stopwatch: Execution Time: 10.48s

**Test Results:**
:




:x: repo_structure_and_cicd: 2/8 pts
   └─ Failed to fetch commits from https://api.github.com/repos/manaboy-07/hng1/commits; No GitHub Actions CI/CD setup found (.github/workflows/*.yml).
:x: rate_limiting: 0/2 pts
   └─ Rate limiting not enforced on /auth/github (expected 429 after 10 requests).

When you run /submit, you'll see fields for three optional tokens: Admin Test Token, Analyst Test Token, and Refresh Test
  Token. Here's how they work:

  Option 1 — Support test_code in your callback (recommended)
  Handle the special code test_code in your /auth/github/callback endpoint. When the grader sends code=test_code with a valid
   state and code_verifier, your app should return a JSON response with access_token and refresh_token for a seeded admin
  user. If this works, you can leave the token fields blank — the grader extracts tokens automatically.

  You will still need to provide the Analyst Test Token since the grader cannot simulate a second role through test_code.

  Option 2 — Paste tokens directly
  If your app doesn't support test_code, generate tokens from your seeded users and paste them into the form:
  - Admin Test Token — a valid JWT/bearer token for a user with the admin role
  - Analyst Test Token — a valid JWT/bearer token for a user with the analyst role
  - Refresh Test Token — the refresh token paired with your admin token